import { notesRoute } from "../../../../lib/timeline-studio/notes-route";

// /ando-grok's composer notes → composer-notes.ando-grok.json at the repo root.
export const { GET, POST } = notesRoute("composer-notes.ando-grok.json");
