import type { ComponentPropsWithRef } from 'react'

/** DownloadPlatforms props: a plain `<section>` (its content is brand-fixed). */
export type DownloadPlatformsProps = ComponentPropsWithRef<'section'>
