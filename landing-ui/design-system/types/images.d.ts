// Static image imports (`.webp`, `.png`, ...) resolve to `next/image`'s
// `StaticImageData`, so component stories can `import x from './x.webp'` and
// pass it straight to <Image>. `next` is a devDependency of this package.
/// <reference types="next/image-types/global" />
